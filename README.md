# Levantar el proyecto
1. correr npm install
2. correr npx react-native run-android
3. Crear variables entorno .env :

REACT_APP_API_URL = http://localhost:3030/api/v1/ =>api del pc que se este corriendo el backend

